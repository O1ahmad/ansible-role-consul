---
- name: Default playbook for testing
  hosts: test-kitchen
  roles:
    - ansible-role-consul
  vars:
    install_type: archive
    consul_configs:
      - name: test-example
        config:
          log_level: debug
    extra_run_args:
      - '-dev'
    custom_unit_properties: {}
